pipeline {
    agent any
    environment {
        CI = 'true'
    }
    stages {
        stage('Build') {
            steps {
                echo 'building stage...'
                bat 'npm install'
                bat 'npm run build'
            }
        }
        
    }
}
