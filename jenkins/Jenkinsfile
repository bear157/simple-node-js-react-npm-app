pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                echo 'building stage...'
                bat 'npm install'
                bat 'npm run build'
                bat 'serve -s build'
                echo 'building stage end...'
            }
        }
        
    }
}
